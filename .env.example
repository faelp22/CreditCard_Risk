# ==============================================================================
# Variáveis de Ambiente - CreditCard Risk Application
# ==============================================================================
# Copie este arquivo para .env e ajuste conforme necessário
# Arquivo .env será ignorado pelo Git (configurado em .gitignore)

# ---------------------------------------------
# Streamlit Configuration
# ---------------------------------------------
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
STREAMLIT_SERVER_MAX_UPLOAD_SIZE=200
STREAMLIT_SERVER_ENABLE_CORS=false
STREAMLIT_SERVER_ENABLE_XSRF_PROTECTION=true

# ---------------------------------------------
# LLM Configuration (Ollama)
# ---------------------------------------------
# URL do servidor Ollama
LLM_BASE_URL=http://192.168.1.2:11434

# Modelo LLM a ser usado
LLM_MODEL=qwen2.5:0.5b

# Timeout para chamadas LLM (segundos)
LLM_TIMEOUT=30

# Número máximo de tentativas
LLM_MAX_RETRIES=2

# Temperatura do modelo (0.0 a 1.0)
LLM_TEMPERATURE=0.7

# Máximo de tokens na resposta
LLM_MAX_TOKENS=500

# ---------------------------------------------
# Model Configuration
# ---------------------------------------------
# Caminho para o modelo treinado
MODEL_PATH=../models/modelo_credito.pkl

# Caminho para o dataset
DATA_PATH=../data/UCI_Credit_Card.csv

# ---------------------------------------------
# Business Rules
# ---------------------------------------------
# Ticket médio por cliente aprovado (R$)
TICKET_MEDIO=500

# Prejuízo médio por inadimplente (R$)
PREJUIZO_MEDIO=1000

# Threshold padrão de decisão (0.0 a 1.0)
DEFAULT_THRESHOLD=0.5

# ---------------------------------------------
# Python Configuration
# ---------------------------------------------
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# ---------------------------------------------
# Logging
# ---------------------------------------------
LOG_LEVEL=INFO
